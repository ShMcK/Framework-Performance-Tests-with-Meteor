import { Binding } from 'angular2/di';
import { MeasureValues } from '../measure_values';
import { Reporter } from '../reporter';
export declare class MultiReporter extends Reporter {
    static createBindings(childTokens: List<any>): List<Binding>;
    _reporters: List<Reporter>;
    constructor(reporters: any);
    reportMeasureValues(values: MeasureValues): Promise<List<any>>;
    reportSample(completeSample: List<MeasureValues>, validSample: List<MeasureValues>): Promise<List<any>>;
}
