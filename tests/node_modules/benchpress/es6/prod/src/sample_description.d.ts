import { Binding } from 'angular2/di';
/**
 * SampleDescription merges all available descriptions about a sample
 */
export declare class SampleDescription {
    id: string;
    metrics: StringMap<string, any>;
    static BINDINGS: List<Binding>;
    description: StringMap<string, any>;
    constructor(id: string, descriptions: List<StringMap<string, any>>, metrics: StringMap<string, any>);
    toJson(): {
        'id': string;
        'description': StringMap<string, any>;
        'metrics': StringMap<string, any>;
    };
}
