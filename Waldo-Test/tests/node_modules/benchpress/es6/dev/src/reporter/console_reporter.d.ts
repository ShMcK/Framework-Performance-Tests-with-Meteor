import { Binding, OpaqueToken } from 'angular2/di';
import { Reporter } from '../reporter';
import { MeasureValues } from '../measure_values';
/**
 * A reporter for the console
 */
export declare class ConsoleReporter extends Reporter {
    private _columnWidth;
    private _print;
    static PRINT: OpaqueToken;
    static COLUMN_WIDTH: OpaqueToken;
    static BINDINGS: List<Binding>;
    static _lpad(value: any, columnWidth: any, fill?: string): string;
    static _formatNum(n: any): string;
    static _sortedProps(obj: any): any[];
    private _metricNames;
    constructor(_columnWidth: number, sampleDescription: any, _print: Function);
    _printDescription(sampleDescription: any): void;
    reportMeasureValues(measureValues: MeasureValues): Promise<any>;
    reportSample(completeSample: List<MeasureValues>, validSample: List<MeasureValues>): Promise<any>;
    _printStringRow(parts: any, fill?: string): void;
}
