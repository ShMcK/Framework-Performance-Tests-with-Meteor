import { Binding } from 'angular2/di';
import { WebDriverExtension, PerfLogFeatures } from '../web_driver_extension';
import { WebDriverAdapter } from '../web_driver_adapter';
/**
 * Set the following 'traceCategories' to collect metrics in Chrome:
 * 'v8,blink.console,disabled-by-default-devtools.timeline'
 *
 * In order to collect the frame rate related metrics, add 'benchmark'
 * to the list above.
 */
export declare class ChromeDriverExtension extends WebDriverExtension {
    private _driver;
    static BINDINGS: List<Binding>;
    constructor(_driver: WebDriverAdapter);
    gc(): Promise<any>;
    timeBegin(name: string): Promise<any>;
    timeEnd(name: string, restartName?: string): Promise<any>;
    readPerfLog(): Promise<any>;
    _convertPerfRecordsToEvents(chromeEvents: List<StringMap<string, any>>, normalizedEvents?: List<StringMap<string, any>>): List<StringMap<string, any>>;
    perfLogFeatures(): PerfLogFeatures;
    supports(capabilities: StringMap<string, any>): boolean;
}
